# UK Planning Pipeline Tracker

Static build of `uk-planning-pipeline-tracker` from the ConstructionCapCloudSites monorepo.

Deployable as a static site on Render, DigitalOcean App Platform, Fly.io, or any root-serving static host — connect this repo, set publish directory to `/`, no build command needed.
